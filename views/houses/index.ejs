
<div class="d-flex justify-content-between align-items-center mb-3">
  <h2>Houses</h2>
  <a href="/houses/new" class="btn btn-primary">Add House</a>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <table class="table table-striped mb-0">
      <thead>
        <tr><th>ID</th><th>House Name</th><th>House Color</th><th>Actions</th></tr>
      </thead>
      <tbody>
        <% houses.forEach((item) => { %>
          <tr>
            <td><%= item.id %></td>
            <td><%= item.house_name %></td>
            <td><%= item.house_color %></td>
            <td>
              <a href="/houses/<%= item.id %>" class="btn btn-sm btn-info">View</a>
              <a href="/houses/<%= item.id %>/edit" class="btn btn-sm btn-warning">Edit</a>
              <a href="/houses/<%= item.id %>/delete" class="btn btn-sm btn-danger">Delete</a>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>
